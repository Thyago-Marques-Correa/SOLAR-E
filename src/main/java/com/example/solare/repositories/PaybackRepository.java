package com.example.solare.repositories;

import com.example.solare.models.Payback;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PaybackRepository extends JpaRepository<Payback, Integer> {
}
